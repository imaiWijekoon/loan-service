# Loan Service

Microservice to manage book loans, checkouts, returns, and overdue tracking.

## Setup

1. `npm install`
2. Create `.env` based on `.env.example`
3. `npm run dev`

## API Documentation

Swagger UI is available at `http://localhost:8083/api-docs`
